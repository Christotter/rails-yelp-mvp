<h1>Add a new restaurant</h1>

<% list = ['chinese', 'italian', 'japanese', 'french', 'belgian'] %>

<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :category, as: :select, collection: list %>
  <%= f.submit :Submit %>
<% end %>
